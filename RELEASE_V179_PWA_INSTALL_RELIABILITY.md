# NeMeSiS SHARK PRO V179

## PWA INSTALL RELIABILITY + PUSH FULL FOUNDATION

Arregla la instalación en Chrome PC y móvil:
- manifest.json en raíz y manifest estático coherente.
- service-worker.js robusto con scope `/`.
- botón inteligente de instalación en landing y cliente.
- se oculta si la app ya está instalada.
- fallback claro cuando Chrome no dispara prompt nativo.
- panel admin `/admin/pwa-install`.
- APIs `/api/v179/pwa-install-status` y `/api/v179/app-install-health`.

Mantiene V178 Automation Engine y toda la base Telegram/Admin/Live anterior.
