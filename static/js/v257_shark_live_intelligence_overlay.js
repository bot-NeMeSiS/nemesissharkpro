window.NMS_LIVE_INTELLIGENCE_OVERLAY_V257={realOnly:true,setPressure(value){const v=Math.max(0,Math.min(100,Number(value)||0));const fill=document.getElementById('v257-pressure-fill');const text=document.getElementById('v257-pressure-text');if(fill)fill.style.width=v+'%';if(text)text.textContent=v>0?`Presión contextual real: ${v}%`:'Esperando eventos, marcador o métricas live reales.'},setDominance(home,away){const h=document.getElementById('v257-home-dom');const a=document.getElementById('v257-away-dom');if(h)h.style.width=Math.max(0,Math.min(100,Number(home)||0))+'%';if(a)a.style.width=Math.max(0,Math.min(100,Number(away)||0))+'%'},setTempo(label){const el=document.getElementById('v257-tempo');if(el)el.textContent=label||'LOW DATA'},setInterest(label){const el=document.getElementById('v257-interest');if(el)el.textContent=label||'WATCH'},addAlert(text){if(!text)return;const box=document.getElementById('v257-alerts');if(!box)return;const empty=box.querySelector('.v257-empty');if(empty)empty.remove();const div=document.createElement('div');div.className='v257-alert';div.textContent=text;box.appendChild(div)}};
